cpp:
  default:
    - clang %s.* -o %s
    - g++ %s.* -o %s
    - gcc %s.* -o %s
cc:
  default:
    - clang %s.* -o %s
    - g++ %s.* -o %s
    - gcc %s.* -o %s
cxx:
  default:
    - clang %s.* -o %s
    - g++ %s.* -o %s
    - gcc %s.* -o %s
py:
  default:
    - 
sh:
  default:
    -

